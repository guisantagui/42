#include "philo.h"
/*
int  check_deaths(t_philo_arg *philo)
{
    int deaths;

    pthread_mutex_lock(&philo->table->any_dead_mutex);
    deaths = philo->table->any_dead;
    pthread_mutex_unlock(&philo->table->any_dead_mutex);
    return (deaths);
}
*/